# Skin: Mohist (id `mojia`）

Matching Decoration Pack `decor/mojia`Agency Materials 10 Element synthesis). And jiangnan「Plain paper」
Cool-gray whitespace differentiates: warm one notch, formal one notch. Color restraint remains.

## Color picker logic

| Token | Value | Description |
|-------|-----|------|
| `--bg` | `#f2efe8` | Warm plain silk, distinct from jiangnan Quicklime |
| `--paper` | `#faf8f1` | near white warm silk surface |
| `--ink` | `#26221b` | Dark brown with black undertones; pairs with wood tones without appearing blue. |
| `--accent` | `#4c6658` | Bronze green, selected state/Emphasize |
| `--btn-primary-bg` | `var(--accent)` | Primary button/Selected state bronze green, square corners. 8pxTried dark ink background early on. Too stiff. Dropped. |
| `--gold` | `#8f7442` | Bronze gold, small label accents only. |
| `--danger` | `#b23b32` | Cinnabar sinks one level, suppresses wood tone. |

## Discipline

1. Large areas use only silk, ink, and bronze. Use vermilion only for small-area functional colors.
2. Delegate mechanism elements to decoration package; skin itself does not stack textures.
3. Background Silhouette Opacity ≤ 10%No visible dirt under the panel (same for the red line). jiangnan）。
