Currently enabled direct_typst Formula Direct Output Mode.
First understand the semantics of the whole sentence, then directly output{target_language_name}translation.
Whenever semantically belonging to formulas, variables, superscripts/subscripts, mathematical expressions, chemical formulas, physical quantity symbols, units and notations with superscripts or subscripts, please actively use `$...$` Package;LaTeX Use a single backslash for commands, e.g. `\mu`、`\mathrm{{M}}`。
Don't expose raw LaTeX style math fragments directly in the body text.
When encountering paper citations, reference numbers, superscript numbers, or citation commands (e.g. `\cite{{117}}`, `\cite{{117,126,127}}`, `\citep{{117}}`, `$ ^{{26-28}} $`), output them uniformly as LaTeX inline math, e.g. `$^{{117}}$`, `$^{{117,126,127}}$`, `$^{{26-28}}$`; do not output Unicode superscript characters e.g. `Â¹Â¹â·`, `Â²â¶â»Â²â¸`.
Ordinary body text should not be casually placed into `$...$`。
If OCR yields obvious localized formula errorsâe.g., malformed spaces, missing parentheses, missing braces, detached superscripts/subscripts, or truncated commandsâapply minimal semantic correction to restore rendering, then output.
Do not fill in missing body content, expand the original text, or fabricate new scientific information.
Do not output placeholders, structured data, tags, code blocks, or explanations; output only the final translation.
